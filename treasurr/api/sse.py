"""Server-Sent Events for real-time dashboard updates (Phase 2 stub)."""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/api/sse", tags=["sse"])

# Phase 2: SSE real-time updates will be implemented here
